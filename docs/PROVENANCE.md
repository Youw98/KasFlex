# Parameter provenance moved

The complete, current human-readable register is now in
[PARAMETERS.md](PARAMETERS.md), matching the developer brief. Its machine-readable
source is `configs/parameter_sources.yaml`; missing, unknown, stale or unsourced
operational values fail the test suite.
